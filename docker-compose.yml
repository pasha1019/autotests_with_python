version: '3.8'

services:
  autotests:
    build: .
    volumes:
      - .:/app
      - ./allure-results:/app/allure-results
    environment:
      - LOGIN=${LOGIN}
      - PASSWORD=${PASSWORD}
    working_dir: /app
    # command: pytest --alluredir=/app/allure-results -v  # опционально